<!DOCTYPE HTML>
<html>
<body>
  <h1>Users:</h1>
  <% users.forEach(function(mate) { %>
  <div>
    First: <%= mate.first_name %> Last: <%= mate.last_name %> id: <%= mate.id %>
  </div>
  <% }) %>
</body>
</html>